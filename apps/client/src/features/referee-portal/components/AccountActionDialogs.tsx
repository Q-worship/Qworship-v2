/** Quiet Momentum account actions: guarded password, device-session, and payout-destination workflows. */
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Check, Chrome, CreditCard, Eye, EyeOff, KeyRound, Laptop, Loader2, MapPin, ShieldCheck, Smartphone, Trash2 } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "../lib/toast";

export function ChangePasswordDialog() {
  const [open, setOpen] = useState(false); const [show, setShow] = useState(false); const [current, setCurrent] = useState(""); const [next, setNext] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const [done, setDone] = useState(false);
  function reset(){setCurrent("");setNext("");setConfirm("");setError("");setSaving(false);setDone(false);setShow(false)}
  function submit(event:FormEvent){event.preventDefault();if(!current){setError("Enter your current password.");return}if(next.length<10){setError("Use at least 10 characters for the new password.");return}if(next!==confirm){setError("The new passwords do not match.");return}setError("");setSaving(true);window.setTimeout(()=>{setSaving(false);setDone(true);toast.success("Password updated",{description:"Other active sessions were notified of this security change."})},650)}
  return <Dialog open={open} onOpenChange={(value)=>{setOpen(value);if(!value)window.setTimeout(reset,200)}}><DialogTrigger asChild><Button variant="outline" className="bg-white">Change password</Button></DialogTrigger><DialogContent className="referee-portal-dialog rounded-[24px] sm:max-w-[500px]">{!done?<><DialogHeader><span className="mb-2 grid h-11 w-11 place-items-center rounded-xl bg-violet-50 text-[#8054F6]"><KeyRound size={20}/></span><DialogTitle className="text-2xl">Change your password</DialogTitle><DialogDescription>Choose a unique password. You will remain signed in on this trusted device.</DialogDescription></DialogHeader><form onSubmit={submit} className="mt-3 space-y-4"><label><Label>Current password</Label><div className="relative mt-2"><Input value={current} onChange={e=>setCurrent(e.target.value)} type={show?"text":"password"} className="h-11 bg-[#f8f7fb] pr-11"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-[#8f8995]" aria-label={show?"Hide passwords":"Show passwords"}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><label><Label>New password</Label><Input value={next} onChange={e=>setNext(e.target.value)} type={show?"text":"password"} className="mt-2 h-11 bg-[#f8f7fb]" placeholder="At least 10 characters"/></label><label><Label>Confirm new password</Label><Input value={confirm} onChange={e=>setConfirm(e.target.value)} type={show?"text":"password"} className="mt-2 h-11 bg-[#f8f7fb]"/></label>{error&&<p className="rounded-xl bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</p>}<div className="rounded-xl bg-[#f7f5ff] p-3 text-xs leading-5 text-[#706a78]"><ShieldCheck className="mr-2 inline text-[#8054F6]" size={15}/>Q-Worship will never ask you to send your password by email or support message.</div><DialogFooter><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" className="violet-button" disabled={saving}>{saving?<><Loader2 className="mr-2 animate-spin" size={16}/>Updating…</>:"Update password"}</Button></DialogFooter></form></>:<div className="py-8 text-center"><span className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-50 text-emerald-600"><Check size={28}/></span><DialogTitle className="mt-5 text-2xl">Password updated</DialogTitle><DialogDescription className="mx-auto mt-2 max-w-sm">Your new password is active. This trusted session remains open.</DialogDescription><Button className="violet-button mt-6 w-full" onClick={()=>setOpen(false)}>Done</Button></div>}</DialogContent></Dialog>
}

type Session={id:string;name:string;detail:string;current?:boolean;icon:typeof Laptop};
const initialSessions:Session[]=[{id:"windows",name:"Chrome on Windows",detail:"Accra, Ghana · Active now",current:true,icon:Laptop},{id:"iphone",name:"Safari on iPhone",detail:"Accra, Ghana · 2 days ago",icon:Smartphone}];
export function TrustedSessionsDialog(){const[open,setOpen]=useState(false);const[sessions,setSessions]=useState(initialSessions);function revoke(id:string){setSessions(items=>items.filter(item=>item.id!==id));toast.success("Device session revoked")}function revokeOthers(){setSessions(items=>items.filter(item=>item.current));toast.success("All other sessions signed out")}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="outline" className="bg-white">Review</Button></DialogTrigger><DialogContent className="referee-portal-dialog rounded-[24px] sm:max-w-[560px]"><DialogHeader><span className="mb-2 grid h-11 w-11 place-items-center rounded-xl bg-violet-50 text-[#8054F6]"><Laptop size={20}/></span><DialogTitle className="text-2xl">Trusted devices</DialogTitle><DialogDescription>Review where your Q-Worship Referrer account is signed in. Remove any device you do not recognise.</DialogDescription></DialogHeader><div className="mt-4 space-y-3">{sessions.map(session=><article key={session.id} className="flex items-center gap-3 rounded-[16px] border border-[#ece8f2] p-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[#f5f2fb] text-[#8054F6]"><session.icon size={19}/></span><div className="min-w-0 flex-1"><div className="flex items-center gap-2 text-sm font-extrabold">{session.name}{session.current&&<span className="rounded-full bg-emerald-50 px-2 py-0.5 text-[9px] font-bold text-emerald-700">This device</span>}</div><div className="mt-1 text-xs text-[#8c8592]">{session.detail}</div></div>{!session.current&&<Button size="icon" variant="ghost" className="text-rose-500 hover:bg-rose-50 hover:text-rose-600" onClick={()=>revoke(session.id)} aria-label={`Revoke ${session.name}`}><Trash2 size={16}/></Button>}</article>)}</div><div className="mt-2 flex gap-2 rounded-xl bg-[#f7f5fb] p-3 text-xs leading-5 text-[#716a77]"><ShieldCheck className="mt-0.5 shrink-0 text-[#8054F6]" size={15}/>Changing your password or revoking a device creates a security notification.</div><DialogFooter className="mt-4"><Button variant="outline" onClick={()=>setOpen(false)}>Close</Button><Button className="violet-button" onClick={revokeOthers} disabled={sessions.length===1}>Sign out other devices</Button></DialogFooter></DialogContent></Dialog>}

export function PayoutMethodDialog(){const[open,setOpen]=useState(false);const[saving,setSaving]=useState(false);const[done,setDone]=useState(false);function submit(event:FormEvent){event.preventDefault();setSaving(true);window.setTimeout(()=>{setSaving(false);setDone(true);toast.success("Payout update submitted",{description:"The new destination is pending secure verification."})},700)}function reset(){setSaving(false);setDone(false)}return <Dialog open={open} onOpenChange={(value)=>{setOpen(value);if(!value)window.setTimeout(reset,200)}}><DialogTrigger asChild><Button className="violet-button">Update payout method</Button></DialogTrigger><DialogContent className="referee-portal-dialog rounded-[24px] sm:max-w-[560px]">{!done?<><DialogHeader><span className="mb-2 grid h-11 w-11 place-items-center rounded-xl bg-violet-50 text-[#8054F6]"><CreditCard size={20}/></span><DialogTitle className="text-2xl">Update payout destination</DialogTitle><DialogDescription>Changes require identity and bank-ownership verification. Existing requests keep their original destination snapshot.</DialogDescription></DialogHeader><form onSubmit={submit} className="mt-3 space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label><Label>Country</Label><Select defaultValue="ghana"><SelectTrigger className="mt-2 h-11 bg-[#f8f7fb]"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ghana">Ghana</SelectItem><SelectItem value="nigeria">Nigeria</SelectItem><SelectItem value="kenya">Kenya</SelectItem><SelectItem value="south-africa">South Africa</SelectItem></SelectContent></Select></label><label><Label>Settlement currency</Label><Select defaultValue="usd"><SelectTrigger className="mt-2 h-11 bg-[#f8f7fb]"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="usd">USD</SelectItem><SelectItem value="ghs">GHS</SelectItem><SelectItem value="ngn">NGN</SelectItem><SelectItem value="zar">ZAR</SelectItem></SelectContent></Select></label></div><label><Label>Account holder</Label><Input required defaultValue="Daniel Mensah" className="mt-2 h-11 bg-[#f8f7fb]"/></label><div className="grid gap-4 sm:grid-cols-2"><label><Label>Bank name</Label><Input required defaultValue="Ecobank" className="mt-2 h-11 bg-[#f8f7fb]"/></label><label><Label>Account number</Label><Input required inputMode="numeric" className="mt-2 h-11 bg-[#f8f7fb]" placeholder="Enter complete number"/></label></div><p className="flex gap-2 rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-800"><MapPin className="mt-0.5 shrink-0" size={14}/>Verification documents and settlement timing may vary by country and payout provider.</p><DialogFooter><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" className="violet-button" disabled={saving}>{saving?<><Loader2 className="mr-2 animate-spin" size={16}/>Submitting…</>:"Continue to verification"}</Button></DialogFooter></form></>:<div className="py-8 text-center"><span className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-50 text-emerald-600"><Check size={28}/></span><DialogTitle className="mt-5 text-2xl">Verification started</DialogTitle><DialogDescription className="mx-auto mt-2 max-w-sm">Your current Ecobank destination remains active until the new payout method is verified.</DialogDescription><Button className="violet-button mt-6 w-full" onClick={()=>setOpen(false)}>Done</Button></div>}</DialogContent></Dialog>}
