/** Quiet Momentum referral hub: attributable sharing tools and a plain-language church pipeline. */
import StatusPill from "../components/StatusPill";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { churches } from "../data/mockData";
import { getReferralCode, getReferralLink } from "../lib/referralCode";
import { ArrowRight, Building2, Check, Copy, Filter, Link2, Plus, QrCode, Search, Send } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "../lib/toast";
import { Link } from "wouter";
import MomentumArtwork from "../components/MomentumArtwork";
import ReferralToolDialogs, { type ReferralTool } from "../components/ReferralToolDialogs";
import { useAuthStore } from "@/features/auth/auth.store";

export default function Referrals() {
  const user = useAuthStore((state) => state.user);
  const referralCode = useMemo(() => getReferralCode(user), [user]);
  const referralLink = useMemo(() => getReferralLink(referralCode), [referralCode]);
  const [search, setSearch] = useState(""); const [status, setStatus] = useState("all"); const [copied, setCopied] = useState(false); const [open, setOpen] = useState(false); const [activeTool, setActiveTool] = useState<ReferralTool>(() => { const tool = new URLSearchParams(window.location.search).get("tool"); const path = window.location.pathname; if (path.endsWith("/qr-code")) return "qr"; if (path.endsWith("/share-message")) return "share"; if (path.endsWith("/campaign-link")) return "campaign"; return tool === "qr" || tool === "share" || tool === "campaign" ? tool : null; });
  const filtered = useMemo(() => churches.filter(c => (c.church + c.city + c.country).toLowerCase().includes(search.toLowerCase()) && (status === "all" || c.status === status)), [search, status]);
  async function copy() { await navigator.clipboard.writeText(referralLink); setCopied(true); toast.success("Referral link copied"); window.setTimeout(() => setCopied(false), 1500); }
  function submit(event: FormEvent) { event.preventDefault(); setOpen(false); toast.success("Church introduction submitted", { description: "Q-Worship will review and contact the church." }); }
  return <div>
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold tracking-[.15em] text-[#8054F6] uppercase">Referral hub</p><h1 className="mt-2 text-[34px] font-extrabold">Share your link. We’ll show you what happens next.</h1><p className="mt-2 text-sm text-[#77727f]">Create an attributable QR code, prepare the right message, run trackable campaigns, or register an offline church introduction.</p></div><Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button className="violet-button h-11 rounded-xl px-5 font-bold"><Plus className="mr-2" size={17} />Introduce a church</Button></DialogTrigger><DialogContent className="referee-portal-dialog rounded-[22px] sm:max-w-[560px]"><DialogHeader><DialogTitle className="text-2xl">Introduce a church</DialogTitle><DialogDescription>Use this when you met the church offline. Q-Worship will check for duplicates before assigning it to you.</DialogDescription></DialogHeader><form onSubmit={submit} className="mt-3 space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label><Label>Church name</Label><Input required className="mt-2 h-11 bg-[#f7f6fa]" placeholder="e.g. Grace Community" /></label><label><Label>Country</Label><Select required><SelectTrigger className="mt-2 h-11 bg-[#f7f6fa]"><SelectValue placeholder="Choose country" /></SelectTrigger><SelectContent><SelectItem value="ghana">Ghana</SelectItem><SelectItem value="nigeria">Nigeria</SelectItem><SelectItem value="kenya">Kenya</SelectItem><SelectItem value="south-africa">South Africa</SelectItem></SelectContent></Select></label></div><div className="grid gap-4 sm:grid-cols-2"><label><Label>Contact name</Label><Input required className="mt-2 h-11 bg-[#f7f6fa]" placeholder="Primary contact" /></label><label><Label>Contact email or phone</Label><Input required className="mt-2 h-11 bg-[#f7f6fa]" placeholder="Verified contact detail" /></label></div><label><Label>Context for the introduction</Label><Textarea className="mt-2 min-h-24 bg-[#f7f6fa]" placeholder="How do you know this church and what are they interested in?" /></label><p className="rounded-xl bg-[#f5f2ff] p-3 text-xs leading-5 text-[#6f687b]">Only submit contacts who expect to hear from Q-Worship. Attribution is confirmed after duplicate and eligibility checks.</p><DialogFooter><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button type="submit" className="violet-button"><Send className="mr-2" size={16} />Submit introduction</Button></DialogFooter></form></DialogContent></Dialog></div>

    <section className="mt-7 grid gap-5 xl:grid-cols-[1.16fr_.84fr]"><article className="surface overflow-hidden rounded-[24px] p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-bold tracking-[.12em] text-[#8d8997] uppercase">Your default link</p><h2 className="mt-2 text-xl font-extrabold">One link for every church journey</h2></div><span className="rounded-lg bg-[#25212d] px-3 py-2 font-mono text-xs font-semibold text-white">{referralCode}</span></div><div className="mt-5 flex flex-col gap-2 rounded-xl bg-[#f5f3fb] p-2 sm:flex-row sm:items-center"><div className="flex min-w-0 flex-1 items-center gap-2 px-2 text-sm text-[#5c5864]"><Link2 size={17} className="shrink-0 text-[#8054F6]" /><span className="truncate">{referralLink}</span></div><Button onClick={copy} className="h-10 rounded-lg bg-white font-bold text-[#322e38] shadow-sm hover:bg-[#eee9ff]">{copied ? <Check className="mr-2 text-emerald-600" size={16} /> : <Copy className="mr-2" size={16} />}{copied ? "Copied" : "Copy link"}</Button></div><div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" className="rounded-lg bg-white" onClick={() => setActiveTool("qr")}><QrCode className="mr-2" size={16} />QR code</Button><Button variant="outline" className="rounded-lg bg-white" onClick={() => setActiveTool("share")}><Send className="mr-2" size={16} />Share message</Button><Button variant="outline" className="rounded-lg bg-white" onClick={() => setActiveTool("campaign")}><Plus className="mr-2" size={16} />Campaign link</Button></div></article><article className="surface-soft relative overflow-hidden rounded-[24px] p-6"><MomentumArtwork variant="growth" /><div className="relative max-w-[62%]"><p className="text-xs font-bold tracking-[.12em] text-[#8054F6] uppercase">This month</p><div className="metric-number mt-3 text-4xl font-extrabold">58</div><p className="mt-1 text-sm font-bold">churches moved forward</p><p className="mt-3 text-xs leading-5 text-[#77717e]">31 began a trial and 18 became active subscribers.</p></div></article></section>

    <section className="surface mt-5 overflow-hidden rounded-[24px]"><div className="flex flex-col gap-4 border-b border-[#efecf5] p-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div><h2 className="text-xl font-extrabold">Referral pipeline</h2><p className="mt-1 text-xs text-[#8a8592]">{filtered.length} of {churches.length} prototype records</p></div><div className="flex flex-col gap-2 sm:flex-row"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-[#aaa6b2]" size={16} /><Input value={search} onChange={e => setSearch(e.target.value)} className="h-10 w-full rounded-xl bg-[#f8f7fb] pl-9 sm:w-64" placeholder="Search churches" /></div><Select value={status} onValueChange={setStatus}><SelectTrigger className="h-10 w-full rounded-xl bg-white sm:w-48"><Filter className="mr-2" size={15} /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem><SelectItem value="Interest received">Interest received</SelectItem><SelectItem value="Trial active">Trial active</SelectItem><SelectItem value="Getting set up">Getting set up</SelectItem><SelectItem value="Trial complete">Trial complete</SelectItem><SelectItem value="Active subscriber">Active subscriber</SelectItem><SelectItem value="Payment attention">Payment attention</SelectItem><SelectItem value="Subscription ended">Subscription ended</SelectItem></SelectContent></Select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left"><thead className="bg-[#faf9fd] text-[10px] font-bold tracking-[.1em] text-[#918c99] uppercase"><tr><th className="px-6 py-3">Church</th><th className="px-4 py-3">Journey</th><th className="px-4 py-3">Source</th><th className="px-4 py-3">Plan</th><th className="px-4 py-3">Introduced</th><th className="px-6 py-3 text-right">Details</th></tr></thead><tbody>{filtered.map(church => <tr key={church.id} className="border-t border-[#f0edf5] text-[13px] transition hover:bg-[#fbfaff]"><td className="px-6 py-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#f0ecff] font-[Manrope] text-xs font-extrabold text-[#8054F6]">{church.church.split(" ").slice(0,2).map(w => w[0]).join("")}</span><div><div className="font-bold text-[#302d35]">{church.church}</div><div className="mt-1 text-[11px] text-[#95909d]">{church.city}, {church.country}</div></div></div></td><td className="px-4 py-4"><StatusPill label={church.status} /></td><td className="px-4 py-4 text-[#6f6a77]">{church.source}</td><td className="px-4 py-4 font-semibold text-[#625e69]">{church.plan}</td><td className="px-4 py-4 text-[#77727f]">{church.date}</td><td className="px-6 py-4 text-right"><Link href={`/referrals/${church.id}`} className="inline-flex items-center gap-1 font-bold text-[#8054F6] hover:underline">Open <ArrowRight size={14} /></Link></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="grid place-items-center px-6 py-16 text-center"><Building2 className="text-[#c8bfdc]" size={32}/><p className="mt-3 font-bold">No matching churches</p><p className="mt-1 text-sm text-[#8b8693]">Adjust the search or status filter.</p></div>}</div></section>
    <ReferralToolDialogs active={activeTool} onClose={() => setActiveTool(null)} referralLink={referralLink} referralCode={referralCode}/>
  </div>;
}
